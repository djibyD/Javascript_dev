//Processing the form validation

function validerName(){
  var nom = document.getElementsByName('nom');
  console.log("nom : " + document.getElementsByName('nom').value);
}

//Retourne le dernier element focus
function getPreviousFocussedElement(){
  var previousElement = document.activeElement;
}
